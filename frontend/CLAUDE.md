# Frontend CLAUDE.md

This is the Claude Code rules file for the frontend component of the Todo Full-Stack Web Application.

## Frontend Specific Rules

- All API calls must include JWT token in Authorization header
- Authentication state must be managed through AuthProvider
- UI must be responsive and work on mobile and desktop
- Form inputs must be validated before submission
- Error states must be handled gracefully with user-friendly messages